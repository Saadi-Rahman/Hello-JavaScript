//use push to add an element to an array as the last element array
var numbers = [78, 45, 98, 22, 75, 19];
numbers.push(25);
console.log(numbers);

//use pop to remove an element to an array as the last element array
console.log(numbers);
numbers.pop();
var element = numbers.pop();
console.log(numbers); 
// console.log(element);

//use unshift to add an element to an array as the first element array
numbers.unshift(25);
console.log(numbers);

//use shift to remove an element to an array as the first element array
numbers.shift();
console.log(numbers);

