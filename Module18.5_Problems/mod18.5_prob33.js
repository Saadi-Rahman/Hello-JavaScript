// Create an array with the names of what you have learned so far in this course. Then output all the elements of that array using a for loop.

var topicNames = ["HTML5", "CSS3", "git", "Bootstrap5", "Tailwind", "JavaScript"];

for(i = 0; i < topicNames.length; i++){
    var topicName = topicNames[i];
    console.log(topicName);
}